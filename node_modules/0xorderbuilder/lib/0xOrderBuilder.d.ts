import { Order, SignedOrder } from "@0xproject/types";
import { BigNumber } from "bignumber.js";
import { EthNetwork } from "./models/EthNetwork";
export declare class ZeroExOrderBuilder {
    static buildQuoteProviderOrder(maker: string, makerTokenAddress: string, takerTokenAddress: string, makerTokenAmount: BigNumber, milisecondsToExpire: BigNumber, relayerUrl: string, network: EthNetwork): Promise<Order>;
    static buildSignedOrder(order: Order, privateKey: string): SignedOrder;
}
