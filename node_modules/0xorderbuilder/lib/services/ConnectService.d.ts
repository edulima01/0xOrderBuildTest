import { TokenPairsItem } from "@0xproject/connect";
import { Order } from "@0xproject/types";
import { BigNumber } from "bignumber.js";
import { EthNetwork } from "../models/EthNetwork";
export declare class ConnectService {
    private httpClient;
    private exchangeContractAddress;
    constructor(relayerUrl: string, network: EthNetwork);
    getTokenPairs(makerTokenAddress: string, takerTokenAddress: string): Promise<TokenPairsItem[]>;
    getOrderWithFee(maker: string, makerTokenAddress: string, takerTokenAddress: string, makerTokenAmount: BigNumber, takerTokenAmount: BigNumber, milisecondsToExpire: BigNumber): Promise<Order>;
    private filterBothPairs(tokenPairs, makerTokenAddress, takerTokenAddress);
    private organizePairs(tokenPairs, makerTokenAddress);
}
