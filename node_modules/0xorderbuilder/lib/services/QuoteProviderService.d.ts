import { Order } from "@0xproject/types";
import { BigNumber } from "bignumber.js";
import "rxjs/add/observable/fromPromise";
import "rxjs/add/operator/mergeMap";
import { EthNetwork } from "../models/EthNetwork";
export declare class QuoteProviderService {
    static buildOrder(maker: string, makerTokenAddress: string, takerTokenAddress: string, makerTokenAmount: BigNumber, milisecondsToExpire: BigNumber, relayerUrl: string, network: EthNetwork): Promise<Order>;
    private static filterByAmount(tokenPairs, makerTokenAmount);
}
